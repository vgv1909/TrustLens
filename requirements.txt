# ============================================================
# TrustLens — Notebook Requirements
# ============================================================
# DTSC 5525 | Girivarshini Varatha Raja, Kishore Dinakaran, Jaya Bharathi Sanjay
#
# These requirements match Notebook Cell 1 exactly and have been tested
# end-to-end on Google Colab (T4 GPU, Python 3.10).
#
# INSTALLATION:
#   pip install -r requirements.txt
#
# For local GPU use, install a torch build matching your CUDA version from:
#   https://pytorch.org/get-started/locally/
#
# If running the notebook in Colab, you do NOT need this file —
# Cell 1 of the notebook installs everything automatically.
# ============================================================

# ─────────────────────────────────────────────────────────────
# CRITICAL: numpy must be 1.26.4
# ─────────────────────────────────────────────────────────────
# The notebook explicitly pins numpy==1.26.4 to avoid the
# "numpy.dtype size changed, binary incompatibility" error that
# occurs when transformers/faiss/scipy wheels were built against
# numpy 1.x but numpy 2.x is installed at runtime.
#
# DO NOT bump this to 2.x without rebuilding the full stack.
numpy==1.26.4

# ─────────────────────────────────────────────────────────────
# Deep learning / NLP
# ─────────────────────────────────────────────────────────────
# torch: install the CUDA-matched build from pytorch.org if using GPU.
# The line below gets the default wheel, which is CPU-only on macOS/Windows
# and CUDA on Linux. Comment it out and install manually if you need a
# specific CUDA version.
torch>=2.1,<3.0

# transformers<5 is intentional — v5 is a major breaking release (Jan 2026)
# that changes weight-loading APIs and requires huggingface_hub>=1.0.
# The notebook uses the v4 APIs (T5Tokenizer, AutoModelForSequenceClassification).
transformers>=4.38,<5.0
accelerate>=0.27
sentence-transformers>=2.5,<3.0
datasets>=2.17

# ─────────────────────────────────────────────────────────────
# Retrieval / evidence
# ─────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4
wikipedia-api>=0.6
diskcache>=5.6

# ─────────────────────────────────────────────────────────────
# Evaluation / visualization
# ─────────────────────────────────────────────────────────────
scikit-learn>=1.3
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
jsonlines>=4.0

# ─────────────────────────────────────────────────────────────
# Interactive demo (for running trustlens_app.py)
# ─────────────────────────────────────────────────────────────
streamlit>=1.30

# ─────────────────────────────────────────────────────────────
# spaCy language model
# ─────────────────────────────────────────────────────────────
# The notebook's Cell 1 installs en_core_web_sm directly from GitHub.
# pip resolves it from the URL below; no separate spacy install needed.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl